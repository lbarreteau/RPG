/*
** EPITECH PROJECT, 2022
** button is clicked
** File description:
** button is clicked
*/

#include "global.h"

int main(void)
{
    if (check_assets() == 84)
        return (84);
    srand(time(NULL));
    set_up_screen();
    return (0);
}
